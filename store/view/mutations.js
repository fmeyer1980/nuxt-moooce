/*
export const setAuthState = (state, newState) => {
    state.authState = newState
};

*/

export const setPageClasses = (state, pageClasses) => {
	state.pageClasses = pageClasses
};


export const setMenuOpen = (state, menuOpen) => {
	state.menuOpen = menuOpen
};
